/** Fresh per-build stamp — visible in window title to confirm non-stale desktop build. */
export const BUILD_ID = `sm-tracks-${new Date().toISOString()}`;

export const APP_WINDOW_TITLE = `Sandbox Music [${BUILD_ID}]`;
